# Example env for docker-compose local development
DATABASE_URL=postgres://postgres:postgres@postgres:5432/pccl
PORT=4000
JWT_SECRET=changeme
FRONTEND_URL=http://localhost:3000

# Service-specific ports (override per service)
IDENTITY_SERVICE_PORT=4001
LEARNING_SERVICE_PORT=4002
CERTIFICATION_SERVICE_PORT=4003
